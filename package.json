{
  "name": "app",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "prepare": "graph codegen && graph build",
    "deploy": "graph deploy --product hosted-service alexandersazonof/liquity"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.53.0",
    "@graphprotocol/graph-ts": "^0.31.0"
  }
}
