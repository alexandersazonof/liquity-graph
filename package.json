{
  "name": "app",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "prepare": "graph codegen && graph build --network matic",
    "prepare:goerli": "graph codegen && graph build --network goerli",
    "prepare:test": "graph codegen && graph build --network polygon-zkevm-testnet",
    "deploy": "graph deploy --product hosted-service alexandersazonof/liquity"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.53.0",
    "@graphprotocol/graph-ts": "^0.31.0",
    "source-map-support": "^0.5.21",
    "@typescript-eslint/eslint-plugin": "^5.42.0",
    "@typescript-eslint/parser": "^5.42.0",
    "eslint": "^8.26.0",
    "eslint-config-prettier": "^8.5.0",
    "matchstick-as": "^0.5.0",
    "prettier": "^2.7.1",
    "typescript": "^4.8.4"
  }
}
